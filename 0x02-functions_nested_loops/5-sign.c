#include "main.h"
/**
 * main -  entry point
 * description: checks code 
 * Return: + if n>0, 0 if n=0, -1 if n<0
 */
int main(void)
{
	int print_sign(int n)
	{

		if (n>0)
		{
			_putchar('+');
			_putchar('\n');
			return (1);
		}

		if else (n == 0)
		{
			_putchar('0');
	                _putchar('\n');
			return (0);
		}

		else (n<0)
		{
			_putchar('-');
			_putchar('/n');
			return (-1);
		}
	}
}
