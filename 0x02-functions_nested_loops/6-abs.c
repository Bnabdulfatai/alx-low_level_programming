#include "main.h"
/**
 *_abs - program computews the absolute value of an integer
 * @n: The integer we want to compute its absolute value
 * Return: 0(sucess)
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
